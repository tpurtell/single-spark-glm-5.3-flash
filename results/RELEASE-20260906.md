# Vision-enabled release profile, 2026-09-06 (Asia/Taipei)

The release launcher now defaults to 262,144 tokens (256K), eight images per
prompt, video disabled, GPU memory utilization 0.87, six scheduler slots,
NVFP4 MLA, DFlash2 K5, ReplaySSM, and prefix caching enabled. Scheduler prefill
batch size is 2048; EXL3 internal prefill capacity is 1024.

EMU was restarted with these defaults and left serving on port 8001. The
published image is unchanged: registry digest `1e91406e…`, local image ID
`b5ae51f7…`. This release changes the host launcher and documentation, not the
container runtime. No workstation GPU was used.

## Current-profile checks

- Single-image color/position checks, including swapped colors: 2/2 passed.
- Eight distinct images in one request: all eight color/position answers
  correct, clean stop. This is a small synthetic image-input smoke test, not
  a broad vision-quality or eight-maximum-resolution-images stress benchmark.
- Code-agent text decode, medians of three runs: C1 26.1482 tok/s; C6 86.4885
  tok/s aggregate, measured over the batch's first-to-last-token window.
- Cold prefill at exactly 32,768 prompt tokens, medians of two timed runs
  after separate warmup: 960.5820 tok/s and 34.1127 s TTFT.
- Strict 32K cold/replay/changed-tail/original test: 4/4 correct. Each warm
  pass recorded 15,360 prefix-cache hits; TTFT 36.2723 / 19.2989 / 19.3062 /
  19.4280 seconds.
- Cancellation: six prefill and six decode disconnects, all 12 exact recovery
  answers passed, final API health HTTP 200.
- Final container state: healthy, no OOM, zero restarts. Startup admitted
  8.11 GiB cache. Two post-ready compilation events (`rotary_kernel` and
  `DSAFusedIndexerKernel`) remain a warmup limitation.
- Ten CPU launcher tests and sixteen other CPU client/fixture tests passed;
  shell syntax and source/documentation whitespace checks passed. Raw serving
  logs retain upstream carriage returns and whitespace unchanged.

Receipts share the prefix `20260906-emu-vision8-256k-`: `vision.json`,
`code-agent.json`, `prefill.json`, `prefix32k.json`, `cancellation.json`,
`environment.json`, `final-state.txt`, and `serving.log`. Environment capture
at 2026-09-05 18:06 UTC records the actual arguments, immutable image,
checkpoint/template provenance, and one GB10 GPU.

The 256K configured limit passed startup admission. This latest short smoke
suite does not itself exercise a full 256K prompt or repeat the full historical
tool/quality matrix. Earlier text-only 262K and 1M tests remain separately
labeled in the README and original qualification log.

## Preserved failed attempts

The requested 512K/2048/.85 vision profile failed admission, not CUDA OOM:

1. `image:1` without an explicit video limit left video implicitly enabled.
   Startup profiled a maximum-size video and admitted only 1.41 GiB cache
   versus 6.10 GiB required. The default now explicitly sets `video:0`.
2. With video disabled and one image enabled, 4.67 GiB cache was available,
   still below 6.10 GiB. The user then selected 256K/.87 and eight images.

Both failed startup logs/states, and the previous server's final log/state,
are preserved. No memory-profiler bypass, automatic utilization increase,
or image-resolution reduction was used.

The first prefill client invocation omitted `/v1` from that client's base URL
and received HTTP 404 before any timed result. Its error log is retained as
`20260906-emu-vision8-256k-prefill-wrong-base-url.log`. The successful run used
`http://emu:8001/v1`; no server restart or result rescoring was involved.
